#!/bin/zsh
set -euo pipefail

cd /Users/robertsale/Code/robdex

listen="${ROBDEX_APP_SERVER_LISTEN:-ws://127.0.0.1:4200}"

codex_bin="/Users/robertsale/.codex/backend/vendor/codex/codex-rs/target/release/codex"

exec "$codex_bin" app-server --listen "$listen"
