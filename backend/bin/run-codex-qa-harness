#!/bin/bash
set -euo pipefail

cd /Users/robertsale/.codex/backend
export PATH="/Users/robertsale/.cargo/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:${PATH:-}"
binary="/Users/robertsale/.codex/backend/target/release/codex-qa-harness"

if [[ ! -x "$binary" ]]; then
  echo "codex-qa-harness release binary is missing: $binary" >&2
  echo "Build it with: cargo build --release -p codex-qa-harness" >&2
  exit 1
fi

exec "$binary" "$@"
