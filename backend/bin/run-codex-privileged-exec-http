#!/bin/bash
set -euo pipefail

cd /Users/robertsale/.codex/backend
export PATH="/Users/robertsale/.cargo/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:${PATH:-}"
binary="/Users/robertsale/.codex/backend/target/release/codex-privileged-exec-http"

if [[ ! -x "$binary" ]]; then
  echo "codex-privileged-exec-http release binary is missing: $binary" >&2
  echo "Build it with: cargo build --release -p codex-privileged-exec-http" >&2
  exit 1
fi

exec "$binary" "$@"
