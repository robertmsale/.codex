#!/bin/zsh
set -euo pipefail

repo_root="${0:A:h:h}"
cd "$repo_root"

export UV_CACHE_DIR="${UV_CACHE_DIR:-/tmp/sync-services-uv-cache}"

exec /opt/homebrew/bin/uv run sync-gitops-http --host "${PARALLELS_SYNC_GITOPS_BIND:-0.0.0.0}" --port "${PARALLELS_SYNC_GITOPS_PORT:-8765}"
