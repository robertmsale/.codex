#!/bin/zsh
set -euo pipefail

repo_root="${0:A:h:h}"
cd "$repo_root"

export UV_CACHE_DIR="${UV_CACHE_DIR:-/tmp/codex-services-uv-cache}"

exec /opt/homebrew/bin/uv run codex-gitops-http --host "${CODEX_GITOPS_BIND:-0.0.0.0}" --port "${CODEX_GITOPS_PORT:-8765}"
