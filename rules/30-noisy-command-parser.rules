# Noisy command enforcement: route through command-parser wrapper.
prefix_rule(
    pattern = ["cargo", ["check", "test", "clippy", "build"]],
    decision = "forbidden",
    justification = "Use command-parser skill: ~/.codex/skills/command-parser/SKILL.md and run ~/.codex/skills/command-parser/scripts/command-parser <command>.",
)
prefix_rule(
    pattern = ["cargo", ["fmt"]],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["dart", ["format"]],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["deno", ["fmt"]],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["pytest"],
    decision = "forbidden",
    justification = "Use command-parser skill: ~/.codex/skills/command-parser/SKILL.md and run ~/.codex/skills/command-parser/scripts/command-parser pytest -q.",
)
prefix_rule(
    pattern = ["flutter", ["analyze", "build"]],
    decision = "forbidden",
    justification = "Use command-parser skill: ~/.codex/skills/command-parser/SKILL.md and run ~/.codex/skills/command-parser/scripts/command-parser <command>.",
)
prefix_rule(
    pattern = ["npm", "run", "build"],
    decision = "forbidden",
    justification = "Use command-parser skill: ~/.codex/skills/command-parser/SKILL.md and run ~/.codex/skills/command-parser/scripts/command-parser npm run build.",
)
prefix_rule(
    pattern = ["xcodebuild"],
    decision = "forbidden",
    justification = "Use command-parser skill: ~/.codex/skills/command-parser/SKILL.md and run ~/.codex/skills/command-parser/scripts/command-parser npm run build.",
)

# Hard ban for formatters.
prefix_rule(
    pattern = ["rustfmt"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["prettier"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["npx", "prettier"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["pnpm", "prettier"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["pnpm", "exec", "prettier"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["yarn", "prettier"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["npm", "exec", "prettier"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["biome", ["format", "check"]],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["npx", "@biomejs/biome"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["dprint", "fmt"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["gofmt"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["goimports"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["gci"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["clang-format"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["swiftformat"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["swift-format"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["black"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["python", "-m", "black"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["python3", "-m", "black"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["yapf"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["autopep8"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["ruff", ["format"]],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["isort"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["docformatter"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["shfmt"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["beautysh"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["perltidy"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["stylua"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["luafmt"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["ktlint", ["-F"]],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["ktfmt"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["scalafmt"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["google-java-format"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["php-cs-fixer"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["phpcbf"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["pint"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["rubocop", ["-A", "-a"]],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["rufo"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["standardrb", ["--fix"]],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["mix", ["format"]],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["elm-format"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["ocamlformat"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["ormolu"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["fourmolu"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["stylish-haskell"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["hindent"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["cabal-fmt"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["nixfmt"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["alejandra"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["terraform", ["fmt"]],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["tofu", ["fmt"]],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["sqlfluff", ["fix"]],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["pg_format"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["taplo", ["fmt", "format"]],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["yamlfmt"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["markdownlint", ["--fix"]],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["mdformat"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["dotnet", ["format"]],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["cmake-format"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["buildifier"],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["eslint", ["--fix"]],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["stylelint", ["--fix"]],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["buf", ["format"]],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["npx", "eslint", ["--fix"]],
    decision = "forbidden",
    justification = "absolutely never run these",
)
prefix_rule(
    pattern = ["npx", "stylelint", ["--fix"]],
    decision = "forbidden",
    justification = "absolutely never run these",
)
