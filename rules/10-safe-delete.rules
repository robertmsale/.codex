# Safe delete enforcement.
prefix_rule(
    pattern = ["rm"],
    decision = "prompt",
    justification = "This command is potentially descructive and must be approved first.",
)
prefix_rule(
    pattern = ["rmdir"],
    decision = "prompt",
    justification = "This command is potentially descructive and must be approved first.",
)
